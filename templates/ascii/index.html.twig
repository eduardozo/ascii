{% extends 'base.html.twig' %}

{% block title %}Tarea 1{% endblock %}

{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }
        table {
            border-collapse: collapse;
        }
        table, th, td {
            border: 1px solid black;
        }
    </style>

    <div class="example-wrapper">
        <h1>Tarea 1</h1>

        <p>Cuando un usuario acceda a la página con la url /ascii/[palabra] devolver una página con
            una tabla de dos columnas donde en cada fila habrá una letra de la palabra dada en la
            primera columna y su correspondiente código ASCII en la segunda columna.</p>
        <table>
            <tr>
                <th>Letra</th>
                <th>ASCII</th>
            </tr>
            {% for column in table %}
                <tr>
                    <td>{{ column.letter }}</td>
                    <td>{{ column.ascii }}</td>
                </tr>
            {% endfor %}
        </table>

    </div>
{% endblock %}
